{
    "identifier": "function_48",
    "description": "Raise matrix A to the power of matrix B.",
    "prompts": [
        {
            "prompt_id": "brief_prompt",
            "prompt": "Create the function's vectorized equivalent using NumPy.",
            "input_code": "def function_48(A, B):\n    n = len(A)\n    result = [0 for _ in range(n)]\n    for j in range(n):\n        result[j] = sum(A[i][j] for i in range(n))\n    return result"
        }
    ],
    "function_prototype": {
        "function_name": "function_48",
        "parameters": [
            {"name": "A", "type": "np.ndarray"},
            {"name": "B", "type": "np.ndarray"}
        ],
        "return_values": [{"type": "np.ndarray"}]
    },
    "tags": ["Array", "Medium"]
}
