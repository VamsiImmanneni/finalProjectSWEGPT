{
    "identifier": "function_54",
    "description": "Calculate the sum of each column in matrices A and B.",
    "prompts": [
        {
            "prompt_id": "brief_prompt",
            "prompt": "Create the function's vectorized equivalent using NumPy.",
            "input_code": "def function_54(A, B):\n    n = len(A)\n    result = [0 for _ in range(n)]\n    for i in range(n):\n        result[i] = max(A[i])\n    return result"
        }
    ],
    "function_prototype": {
        "function_name": "function_54",
        "parameters": [
            {"name": "A", "type": "np.ndarray"},
            {"name": "B", "type": "np.ndarray"}
        ],
        "return_values": [{"type": "np.ndarray"}]
    },
    "tags": ["Array", "Medium"]
}
